<?php
print "abc";

#print "مطلوب اسم العميل";
/*
include "PoParser.php";

$language = "ar"; # for example

$poparser = new Sepia\PoParser();
$entries = $poparser->parse("$language.po");
foreach ($entries as $entry) {
  $src = $entry['msgid'][0];
  $dst = $entry['msgstr'][0];
  if ($src && !$dst) { // no translation: translate it
    $dst = translate($src, "en", $language);
print $dst;
    $poparser->updateEntry($src, $dst);
  }
}
#$poparser->write("/var/www/html/smartUp/local/po-auto-translate/tmp/$language-translated.po");
#system("cat /var/www/html/smartUp/local/po-auto-translate/tmp/$language-translated.po");

function translate($word, $languageFrom = 'en', $languageTo) {
  return "مطلوب اسم العميل";
}

#fr.po:            GNU gettext message catalogue, ASCII text
#fr-translated.po: GNU gettext message catalogue, ISO-8859 text
*/
?>